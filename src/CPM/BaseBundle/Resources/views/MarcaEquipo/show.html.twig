{% extends '::base.html.twig' %}

{% block body -%}
    <h1>MarcaEquipo</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Descripcion</th>
                <td>{{ entity.descripcion }}</td>
            </tr>
        </tbody>
    </table>

        <div class="form-actions">
        <a href="{{ path('marcaequipo') }}" class="btn btn-mini">
            Cancelar        </a>
        <a href="{{ path('marcaequipo_edit', { 'id': entity.id }) }}" class="btn btn-primary btn-mini">
            Editar
        </a>
{{ form(delete_form) }}
{% endblock %}
